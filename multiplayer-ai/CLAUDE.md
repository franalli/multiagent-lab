# multiplayer-ai/CLAUDE.md -- pointer stub (NOT the ramp guide)

> **Role: POINTER STUB.** This file is **not** the source of truth.
> The canonical agent ramp lives at the repo root:
> **[`../CLAUDE.md`](../CLAUDE.md)**. Read that before touching code.
>
> This file exists only so the Convex CLI (`npx convex ai-files
> install/update`) has a place to write the auto-managed block below.
> Do not add ramp content, conventions, or recipes here — they will
> drift from `../CLAUDE.md`. See the [Documentation file
> map](../CLAUDE.md#documentation-file-map) in the root for how the
> four CLAUDE/AGENTS files relate.

<!-- convex-ai-start -->

This project uses [Convex](https://convex.dev) as its backend.

When working on Convex code, **always read
`convex/_generated/ai/guidelines.md` first** for important guidelines on
how to correctly use Convex APIs and patterns. The file contains rules that
override what you may have learned about Convex from training data.

Convex agent skills for common tasks can be installed by running
`npx convex ai-files install`.

<!-- convex-ai-end -->

The Convex CLI section above is auto-managed -- do not edit between the
`convex-ai-start` / `convex-ai-end` markers; it will be regenerated by
`npx convex ai-files install/update`.
